<p>{{ __('exercises/2_29.description.1') }}</p>
<pre><code>(define (make-mobile left right)
  (list left right))
</code></pre>
<p>{{ __('exercises/2_29.description.2') }}</p>
<pre><code>(define (make-branch length structure)
  (list length structure))
</code></pre>
<p>{{ __('exercises/2_29.description.3') }}</p>
<p>{{ __('exercises/2_29.description.4') }}</p>
<p>{{ __('exercises/2_29.description.5') }}</p>
<p>{{ __('exercises/2_29.description.6') }}</p>
<pre><code>(define (make-mobile left right)
  (cons left right))
(define (make-branch length structure)
  (cons length structure))
</code></pre>
<p>{{ __('exercises/2_29.description.7') }}</p>
