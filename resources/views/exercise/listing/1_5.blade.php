<p>{{ __('exercises/1_5.description.1') }}</p>
<pre><code>(define (p) (p))

(define (test x y)
    (if (= x 0)
        0
        y))</code></pre>
<p>{{ __('exercises/1_5.description.2') }}</p>
<pre><code>(test 0 (p))</code></pre>
<p>{{ __('exercises/1_5.description.3') }}</p>
