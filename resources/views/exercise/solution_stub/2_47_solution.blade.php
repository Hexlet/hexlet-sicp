(define (origin-list f) (car f))
(define (edge1-list f) (cadr f))
(define (edge2-list f) (caddr f))

(define (origin-cons f) (car f))
(define (edge1-cons f) (cadr f))
(define (edge2-cons f) (cddr f))
