<div class="text-center mb-3">
    <a class="btn btn-light" href="{{ route('ratings.index') }}">
        {{ __('rating.page_title') }}
    </a>
    <a class="btn btn-light" href="{{ route('ratings.comments') }}">
        {{ __('rating.page_title_comments') }}
    </a>
</div>
